/* exported getValue */
// creating a function getValue with two parameters: object, and key
// return the value of the given object and the given key

function getValue(object, key) {
  return (object[key]);

}
