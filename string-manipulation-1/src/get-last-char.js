/* exported getLastChar */
// defiining a function that gets the last character of a string
// return the character by calling the index of the string.length minus 1

function getLastChar(string) {
  return string[string.length - 1];
}
