/* exported last */

// defining a function that returns the last index of an array
// setting a variable for the last index number with the value of index.length - 1
// returning the array with the last variable

function last(array) {
  var last = array.length - 1;
  return array[last];
}
